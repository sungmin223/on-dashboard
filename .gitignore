node_modules/
.DS_Store
*.log
.vercel
.netlify

# 로컬 작업/백업 산출물 (배포 제외)
_tmp*/
dashboard_backup_*.html

.vercel

# ===== Stage 4 (Vite+React + 백엔드 + 실데이터) =====
# 비밀키 — 절대 커밋 금지
.env
.env.local
.env.*.local

# 빌드 산출물
client/dist/

# 실데이터(내부 단가/재고 보호) — npm run import-data 로 로컬 생성
data/inventory.json
# 영업 시드(제품 단가 포함) — npm run build:sales 로 로컬 생성, Vercel 번들엔 포함
data/sales-seed.json
# 운영 실데이터 CSV(거래처/납품/페어링/지역선호) — 로컬에서만, 빈양식/README만 저장소 포함
data/real/*.csv
data/source/*
!data/source/.gitkeep

